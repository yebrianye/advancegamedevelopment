using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SaveManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {


    /*

         
        if (PlayerPrefs.HasKey("b1")) building.SetActive(true);

        if (PlayerPrefs.HasKey("b2")) building.SetActive(true);

        if (PlayerPrefs.HasKey("b3")) building.SetActive(true);

        if (PlayerPrefs.HasKey("b4")) building.SetActive(true);

        UI.text = PlayerPrefs.GetInt("gold"); ;


        PlayerPrefs.SetInt("b1", 0);

        PlayerPrefs.SetInt("gold",0);

        int currentGold = PlayerPrefs.GetInt("gold");
        currentGold += 290;

        PlayerPrefs.SetInt("gold", currentGold);
        
    */

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
